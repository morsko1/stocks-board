import React from 'react';
import './index.css';

const HomeView = props => {
    return (
        <div className={'home-container'}>
            home
        </div>
    );
}

export default HomeView;
